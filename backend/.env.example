# Copy to .env and adjust as needed for local dev.
APP_ENV=development
DATABASE_URL=postgresql+psycopg://musper:musper@localhost:5434/musper
# Allowed origins for CORS (comma-separated).
CORS_ORIGINS=http://localhost:5173
